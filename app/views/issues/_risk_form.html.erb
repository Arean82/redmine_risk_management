<%= labelled_fields_for(@issue) do |f| %>
  <p>
    <%= f.label :risk_id, t('label_risk') %>
    <%= f.select :risk_id, @project.risks.map { |r| ["#{r.title} (##{r.id})", r.id] }, include_blank: true %>
  </p>
<% end %>